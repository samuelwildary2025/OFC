{
  "name": "whatsapp-api",
  "version": "1.0.0",
  "description": "API WhatsApp não-oficial com painel administrativo",
  "main": "dist/server.js",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:dedupe:wamessageid": "node --input-type=module -e \"import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); await prisma.$executeRawUnsafe(\\\"WITH ranked AS ( SELECT id, \\\\\\\"waMessageId\\\\\\\", row_number() OVER (PARTITION BY \\\\\\\"waMessageId\\\\\\\" ORDER BY \\\\\\\"createdAt\\\\\\\" DESC, id DESC) AS rn FROM \\\\\\\"messages\\\\\\\" WHERE \\\\\\\"waMessageId\\\\\\\" IS NOT NULL ) UPDATE \\\\\\\"messages\\\\\\\" m SET \\\\\\\"waMessageId\\\\\\\" = NULL FROM ranked r WHERE m.id = r.id AND r.rn > 1;\\\"); await prisma.$disconnect();\"",
    "db:push": "prisma db push --accept-data-loss",
    "db:seed": "tsx prisma/seed.ts",
    "db:studio": "prisma studio",
    "lint": "eslint src --ext .ts",
    "test": "vitest",
    "test:integration": "vitest run --config vitest.integration.config.ts"
  },
  "keywords": [
    "whatsapp",
    "api",
    "baileys",
    "nodejs"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2.4.3",
    "bullmq": "^5.30.1",
    "dotenv": "^16.4.5",
    "hono": "^4.6.12",
    "ioredis": "^5.4.1",
    "jose": "^5.9.6",
    "link-preview-js": "^3.0.5",
    "mime-types": "^2.1.35",
    "nanoid": "^5.0.8",
    "pino": "^9.5.0",
    "pino-pretty": "^13.0.0",
    "proxy-chain": "^2.7.0",
    "ws": "^8.18.3",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/mime-types": "^2.1.4",
    "@types/node": "^22.10.2",
    "@types/qrcode": "^1.5.5",
    "@types/ws": "^8.18.1",
    "prisma": "^5.22.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
